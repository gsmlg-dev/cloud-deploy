name: Deploy NixOS

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Target host to deploy'
        required: true
        default: 'all'
        type: choice
        options:
          - vultr-02
          - vultr-03
          - vultr-04
          - all
      build_on_target:
        description: 'Build on target host instead of runner'
        required: false
        default: false
        type: boolean

env:
  NIX_CONFIG_REPO: git@github.com:Gao-OS/nix-config.git
  SSH_USER: gao
  DOMAIN: gsmlg.net

jobs:
  deploy:
    name: Deploy to ${{ matrix.host }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        host: ${{ fromJSON(github.event.inputs.target == 'all' && '["vultr-02", "vultr-03", "vultr-04"]' || format('["{0}"]', github.event.inputs.target)) }}

    steps:
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main
        with:
          extra-conf: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: |
            ${{ secrets.SSH_PRIVATE_KEY }}
            ${{ secrets.NIX_CONFIG_DEPLOY_KEY }}

      - name: Configure SSH known hosts
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KNOWN_HOSTS }}" >> ~/.ssh/known_hosts
          ssh-keyscan github.com >> ~/.ssh/known_hosts 2>/dev/null
          chmod 644 ~/.ssh/known_hosts

      - name: Clone NixOS configuration
        run: |
          git clone ${{ env.NIX_CONFIG_REPO }} nix-config
          cd nix-config
          echo "Cloned nix-config at commit: $(git rev-parse HEAD)"

      - name: Deploy to ${{ matrix.host }}
        working-directory: nix-config
        run: |
          TARGET="${{ env.SSH_USER }}@${{ matrix.host }}.${{ env.DOMAIN }}"
          echo "::group::Deploying to $TARGET"

          if [ "${{ github.event.inputs.build_on_target }}" == "true" ]; then
            nixos-rebuild switch \
              --flake .#${{ matrix.host }} \
              --target-host "$TARGET" \
              --build-host "$TARGET" \
              --use-remote-sudo \
              --verbose
          else
            nixos-rebuild switch \
              --flake .#${{ matrix.host }} \
              --target-host "$TARGET" \
              --use-remote-sudo \
              --verbose
          fi

          echo "::endgroup::"

      - name: Verify deployment
        run: |
          ssh ${{ env.SSH_USER }}@${{ matrix.host }}.${{ env.DOMAIN }} 'nixos-version && uptime'
